# 03 — 执行计划（AI 接手开发指南）

| 项目 | 内容 |
| --- | --- |
| 文档版本 | V1.0 |
| 用途 | 任何新会话（人或 AI）读完本文档即可接手开发，无需重新讨论设计 |
| 前置文档 | [README](../README.md) → [01-需求规格说明书](./01-需求规格说明书.md)（SRS）→ [02-结构与类设计](./02-结构与类设计.md) |

---

## 0. 给接手者的 30 秒摘要

- **做什么**：视觉功能组件库 Visual + WPF 桌面端。本期只做**测距模块**（圈选目标 → 标定 → 实时输出 mm）。
- **当前状态**：只有文档，**尚无代码**。第一个任务从 M0 工程骨架开始。
- **最重要的一条**：shared 层与 modules 层是纯类库，**禁止引用任何 UI 框架**；对外只暴露 Contracts（接口+DTO），实现类 `internal`。
- **做完一个任务**：当场更新本文档 §7 任务状态表（状态 + 完成日期 + 提交号）再提交代码，方便下个会话接着干。

---

## 1. 开发前必读（按顺序）

1. `README.md` — 项目是什么、结构概述、测距模块用法示例；
2. SRS §3 约束（C-01~C-15）+ §3.4 落地保障 — 不可违反的红线；
3. SRS §4.5 判定标准 — 拿不准某功能归基础还是业务时查这个；
4. 02 文档 — 类名/方法/作用的对照表，**编码以此为准**，有出入时改代码也改文档。

## 2. 环境准备

| 项 | 要求 |
| --- | --- |
| OS | Windows 10/11 x64 |
| SDK | .NET 10 SDK（`dotnet --version` 验证） |
| 硬件 | USB 摄像头（M5/M6 阶段需要；M0~M4 可用图片/视频文件代替） |

NuGet 依赖（按工程）：

| 工程 | 包 |
| --- | --- |
| shared 三个 | `OpenCvSharp4`、`OpenCvSharp4.runtime.win` |
| Visual.Distance | 同 shared（无新增） |
| Visual.App / AppCore | + `CommunityToolkit.Mvvm`、`MahApps.Metro`、`MaterialDesignThemes`、`Microsoft.Extensions.DependencyInjection` |
| tests/* | `xunit`、`xunit.runner.visualstudio`、`Microsoft.NET.Test.Sdk`、`NetArchTest.Rules`（仅 Architecture.Tests） |

## 3. 编码约定

- 所有工程：`<Nullable>enable</Nullable>`、`<ImplicitUsings>enable</ImplicitUsings>`、`<LangVersion>latest</LangVersion>`；
- 公共 API：异步方法 `Async` 后缀，带 `CancellationToken ct = default`；
- 目录即命名空间：`Contracts/` 放对外类型（public），`Internal/` 放实现（internal）；
- `InternalsVisibleTo` 仅授予对应 `*.Tests` 工程；
- 提交信息：`feat/fix/docs/test: 简述`（参考 git log 风格）。

## 4. 里程碑与任务分解

> 每个任务的"完成定义（DoD）"= 产物存在 + 对应测试绿 + 文档同步。任务状态维护在 §7。

### M0 — 工程骨架

| # | 任务 | DoD |
| --- | --- | --- |
| 0.1 | 建 `Visual.sln` 与 8 个工程（结构见 README/02），配置项目引用关系（依赖方向单向） | `dotnet build` 通过 |
| 0.2 | 建 `Directory.Build.props`：统一 Nullable/ImplicitUsings/输出目录 | 各 csproj 无重复配置 |
| 0.3 | 建 `Visual.Architecture.Tests`，先写 3 条断言：①shared/modules 不引用 `PresentationFramework`；②public 类型仅在 `*.Contracts` 命名空间；③模块间互不引用 | 断言当前全绿（空工程下也成立），后续任务不得改红 |

### M1 — Visual.Abstractions

| # | 任务 | DoD |
| --- | --- | --- |
| 1.1 | 实现 02 文档 §1 全部类型：`FramePacket`/`RoiRect`/`Point2D`/`DistanceUnit`/`VisionResultBase`/`VisionException`/`VisionErrorCode`/`IVisionModule` | 编译通过；`RoiRect.ClampTo` 有单测（越界收敛正确） |

### M2 — Visual.IO

| # | 任务 | DoD |
| --- | --- | --- |
| 2.1 | `IFrameSource` 契约 + `FrameSourceFactory` | 编译通过 |
| 2.2 | `ImageFolderSource`（测试先行） | IO.Tests 绿：帧号连续、时间戳递增 |
| 2.3 | `VideoFileSource`（循环/暂停/单帧） | 测试绿：Seek 后帧号正确 |
| 2.4 | `UsbCameraSource` + 掉线 `SourceFailed` | 手测：拔摄像头触发事件，进程不崩 |

### M3 — Visual.VisionBase

| # | 任务 | DoD |
| --- | --- | --- |
| 3.1 | 预处理算子 5 个 + `PreprocessPipeline` | 样图断言输出（尺寸/通道/二值效果） |
| 3.2 | `BlobAnalyzer` + `TemplateMatcher` | 样图断言 Blob 数量/位置误差 < 2px |
| 3.3 | `TargetDetectorBase` + `ContourTargetDetector`（本期默认） | 样图 ROI 内检出率 100%（自备 ≥ 10 张样图） |
| 3.4 | `ITracker` + `OpenCvTracker`（CSRT） | 视频样本跟踪 100 帧，丢失率 < 5% |
| 3.5 | 缺陷类算子（P1，可延后） | 列入 §7 状态表即可，本期可不实现 |

### M4 — Visual.Distance + 可拆性验证 ★ 核心里程碑

| # | 任务 | DoD |
| --- | --- | --- |
| 4.1 | Contracts 全部类型（`IDistanceService`/`DistanceServiceBuilder`/`DistanceMeasurement`/`CalibrationInfo`/`NotCalibratedException`） | 架构测试绿 |
| 4.2 | Internal：`MonocularCalibrator`/`DistanceConverter`/`RoiTargetTracker`/`DistanceServiceImpl` | 单元测试绿：样图 + 已知标定，误差 ≤ ±10%（SRS NFR-02） |
| 4.3 | 未标定/未检出分支 | 测试断言：抛 `NotCalibratedException`；未检出 `Distance=null` |
| 4.4 | `Observe(...)` 实时模式 | 测试：VideoFileSource 驱动，每秒产出 ≥ 15 条结果（720p 样本） |
| 4.5 | **ConsoleDemo**（仅引用 DLL，无 UI） | **SRS 验收 S4**：新机器 `dotnet run` 直接跑通 |
| 4.6 | 分发说明（DLL 清单 + 依赖 + 最小调用片段） | 放 `samples/` 下 README |

### M5 — 软件壳（Visual.AppCore / App）

| # | 任务 | DoD |
| --- | --- | --- |
| 5.1 | DI 容器 + `MainViewModel`（视频源选择/播放控制/状态栏） | SRS S1 通过 |
| 5.2 | `RoiInteractionController` + `OverlayRenderer` | SRS S2 通过（<16×16 拒绝、越界收敛） |
| 5.3 | `CalibrationViewModel` + `AppSettingsService`（标定持久化） | SRS S3 通过（重启免重标） |
| 5.4 | 实时闭环：订阅 `Observe` → 叠加渲染 | SRS S1 完整闭环 |

### M6 — 验收

| # | 任务 | DoD |
| --- | --- | --- |
| 6.1 | 按 SRS §9 执行 S1~S8 并记录 | 验收表逐项签认；**S4/S5 是核心项** |
| 6.2 | 2 小时稳定性（S8）+ 性能统计（S7） | 数据写入验收记录 |

## 5. 验证命令速查

```powershell
dotnet build                                    # 全量编译
dotnet test                                     # 全部测试（含架构断言）
dotnet run --project samples/Visual.Distance.ConsoleDemo -- <视频路径>   # 可拆性验证
dotnet run --project src/app/Visual.App         # 启动桌面端
```

## 6. 新会话接手流程（AI/人都一样）

1. 读本文档 §0~§1，再读 02 文档对应模块章节；
2. `git log --oneline -5` 看最近进展；查 §7 状态表找第一个 `⬜ 待开始` 任务；扫一眼 [ISSUE](./ISSUE.md) 中 `🔴/🔨` 状态的问题；
3. `dotnet test` 确认当前全绿（不绿先修，不急着做新任务）；
4. 开发 → 测试 → **先把完成结果反馈到 §7 状态表**（状态、日期、提交号、遗留问题）→ 再提交代码（信息含任务号，如 `feat: M2.3 VideoFileSource`）；状态表与代码必须在同一提交内更新，不允许"代码做完了表没改"；
5. 过程中遇到的问题按格式记入 [ISSUE](./ISSUE.md)，解决后补原因、方案与提交号。

## 7. 任务状态表（跨会话交接的唯一事实源）

**维护规则**：任务开始时标 `🔨`；完成时标 `✅` 并在备注写完成日期 + 提交号；有遗留事项写进备注或转 ISSUE。状态表必须与对应代码在**同一提交**中更新。

| 里程碑 | 任务 | 状态 | 备注（日期 / 提交号 / 遗留） |
| --- | --- | --- | --- |
| M0 骨架 | 0.1~0.3 | ⬜ 待开始 | |
| M1 Abstractions | 1.1 | ⬜ 待开始 | |
| M2 IO | 2.1~2.4 | ⬜ 待开始 | |
| M3 VisionBase | 3.1~3.4 | ⬜ 待开始 | 3.5 缺陷算子本期可缓 |
| M4 Distance ★ | 4.1~4.6 | ⬜ 待开始 | 4.5 ConsoleDemo 是核心验收 S4 |
| M5 软件壳 | 5.1~5.4 | ⬜ 待开始 | |
| M6 验收 | 6.1~6.2 | ⬜ 待开始 | S4/S5 为核心项 |

状态标记：`⬜ 待开始` / `🔨 进行中` / `✅ 完成` / `⏸ 暂缓`

---

## 变更记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| V1.0 | 2026-07-18 | 初版：M0~M6 任务分解 + 接手流程 + 状态表 |
