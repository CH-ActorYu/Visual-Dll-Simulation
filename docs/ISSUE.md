# 问题记录（ISSUE）

开发、联调、验收过程中遇到的问题统一记录在此，便于回溯与跨会话交接。

## 记录规范

- 每行一个问题，编号递增（I-001、I-002……），**不删除已关闭的问题**；
- 状态：`🔴 待处理` / `🔨 处理中` / `✅ 已解决` / `⏸ 挂起`；
- 严重度：`P0 阻塞开发` / `P1 影响功能` / `P2 体验或边缘情况`；
- 解决后必须填写"原因"与"解决方案"，并注明关联提交号；
- 新会话接手时，先扫一眼 `🔴/🔨` 状态的问题再开工。

## 问题列表

| 编号 | 日期 | 严重度 | 状态 | 模块 | 问题描述 | 原因 | 解决方案 | 关联提交 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| I-001 | 2026-07-19 | P1 | ✅ 已解决 | 分发/S4 | 原始 DLL 引用消费者无法加载 `OpenCvSharpExtern` | 没有 NuGet `.deps.json` runtime 资产解析时，仅保留 `runtimes/win-x64/native` 不会加入原生 DLL 搜索路径 | S4 消费者把原生 DLL 同步复制到可执行目录，并在分发说明中固化要求 | 本提交 |
| I-002 | 2026-07-19 | P1 | ⏸ 挂起 | 验收/S5 | HardwareModularWorkflow 尚未接入 Visual，S5 无法签认 | 当前明确不修改相邻仓库，真实宿主集成不属于本轮变更范围 | 需求、接口映射、实施步骤和验收条件已转入 `docs/待处理/HardwareModularWorkflow-S5集成.md` | 本提交 |
| I-003 | 2026-07-19 | P1 | ✅ 已解决 | 验收/S8 | 前四次长稳因运行器偏差、单点采样或稳定预热不足未达到 <10% 验收线 | 第 4 次确认运行期在约 30 分钟形成资源平台；帧池租还始终闭合，不是按帧持续泄漏 | 第 5 次预热 30 分钟后正式统计两小时，Private 窗口仅 +0.53%，池租还 190,386/190,386，S8 Pass | 本提交 |
| I-004 | 2026-07-20 | P2 | ✅ 已解决 | Visual.App/ROI | 左键拖动框选时看不到过程，松开后也不清楚 ROI 是否已选定 | 画布只处理按下/松开，诊断文字又会被下一帧覆盖；未显示画面时失败路径没有提示 | 增加拖动虚线框、持久高亮边框与“ROI 已选定”标签；无画面时提示先启动视频 | 本提交 |
| I-005 | 2026-07-20 | P1 | 🔴 待处理 | Visual.App/实时预览 | 软件启动视频后界面卡死，进程内存曾达到约 1.5 GB，窗口关闭后可能仍残留进程 | `StartAsync` 的逐帧循环运行在 WPF Dispatcher 上；帧缓冲有数据时异步枚举可同步连续完成，每帧又执行测距和 `frame.Data.ToArray()`，造成 UI 饥饿与约 55 MB/s 的预览分配压力 | 待将采集/识别移到后台、UI 仅接收最新快照并节流，复用预览缓冲，同时为停止/关闭增加超时 | 待提交 |
| I-006 | 2026-07-20 | P1 | 🔴 待处理 | Visual.App/目标预览 | ROI 选定内容应显示在视频旁侧，并以目标真实不规则形状呈现，而不是在视频上显示矩形选定标签 | 当前 UI 仅绘制矩形叠加；Blob 检测虽取得轮廓，但 `TargetCandidate` 和 `DistanceMeasurement` 只保留外接矩形，轮廓在引擎层被丢弃 | 待区分“用户圈定的搜索区域”和“算法输出的目标轮廓”：支持套索/多边形约束，契约传递紧凑轮廓，侧栏以透明掩膜裁出目标；实施前先解决 I-005，预览只取最新结果并节流 | 待提交 |

<!-- 示例（填写时删除此行注释）：
| I-001 | 2026-07-20 | P1 | ✅ 已解决 | Visual.IO | 摄像头拔出后 `ReadFramesAsync` 未结束且状态未变化 | 采集循环未检测 Read 失败返回值 | CameraSource 增加连续失败计数并转换为 `Reconnecting`/`Failed` 状态 | a1b2c3d |
-->
