# M7.1 S1～S8 首轮验收记录

## 1. 基线

| 字段 | 内容 |
| --- | --- |
| 日期/执行人 | 2026-07-19 / Codex 自动化执行 |
| 被测提交 | `88943d33ea09ae2482c3f54aa3b9ae97c975c01c` |
| OS | Windows 11 家庭版 10.0.26200 |
| SDK/runtime | .NET SDK 10.0.302 / Microsoft.NETCore.App 10.0.10 |
| CPU/内存 | Intel Core i7-9750H / 31.9 GB |
| 引擎 | OpenCvSharp 4.13.0.20260627，Release x64，无调试器 |
| 设备 | 本轮无指定 USB 相机、物理距离工装和近/中/远数据集 |
| 宿主基线 | HardwareModularWorkflow `5e23db8897250d67378605d5962e7371cf73abaa`，工作区干净，尚无 Visual 引用 |

## 2. 验收结论

| 场景 | 自动化/检查结果 | 正式结论 | 后续条件 |
| --- | --- | --- | --- |
| S1 软件闭环 | WPF Release 构建和 `--smoke-test` 的 DI、XAML、释放链路通过；S4 视频算法闭环通过 | **Blocked** | 接入真实摄像头，人工完成圈选、标定、目标移入/移出观察并截图 |
| S2 测距精度 | 单目公式与生成视频 1000 mm 点通过；没有 3 个物理距离点各 10 次的数据 | **Blocked** | 准备近/中/远工装和原始记录，计算平均值、最大值、标准差、有效样本数 |
| S3 标定持久化 | `AppSettingsServiceTests` 配置和 ROI JSON 往返通过；软件启动路径会调用恢复服务 | **Blocked** | 在真实 UI 完成一次标定、退出、重启并截图确认自动加载 |
| **S4 DLL 独立可用** | 全新 `S4.ConsoleConsumer` 无 ProjectReference/PackageReference；仅引用分发 DLL，30/30 帧 Valid，平均 `1000.00 mm`，无 WPF/WinForms 引用 | **Pass** | 保留脚本作为回归门；分发说明必须包含原生 DLL 输出根目录要求 |
| **S5 宿主集成** | 只读扫描 HardwareModularWorkflow，Visual 引用数为 0；当前决定不修改该仓库 | **Blocked / 挂起** | 已转入[独立待处理文件](../待处理/HardwareModularWorkflow-S5集成.md)，后续单独授权后再实施 |
| S6 异常鲁棒 | 自动化已覆盖未标定、不可用相机 `DeviceLost`、帧源 fatal error、取消和租约清理 | **Blocked** | 真实采集中拔相机并验证 UI 提示、重连/停止和进程不崩溃；补坏视频 UI 操作证据 |
| S7 性能 | 720p、预热 30 秒、正式统计 600 秒；12,750 帧、21.25 次/秒、平均 8.38 ms、P95 10.41 ms、P99 13.03 ms、丢帧 0 | **Pass** | 详见 [M7.2 S7 性能验收记录](./2026-07-19-M7.2-S7.md) |
| S8 稳定性 | 第 3 次运行满两小时且帧池 153,164 次租还完全闭合；单点首末 +10.86% 仍失败，曲线诊断后改用首尾各 10 分钟均值并等待第 4 次完整复测 | **Fail / 复测中** | 详见[第 3 次记录](./2026-07-19-M7.2-S8-第3次.md)；第 1/2 次记录保留历史原因 |

当前 S1～S8：`Pass=2`、`Fail=1`、`Blocked=5`。因此 AC-M1 尚未通过；S4、S7 已通过，S8 第 1 次失败并等待完整复测，核心项 S5 按当前决定挂起。

## 3. S4 执行证据

命令：

```powershell
./tools/acceptance/Invoke-S4.ps1
```

关键输出：

```text
S4 frames=30 valid=30 averageMm=1000.00 uiReference=none
S4_PASS
```

分发关键文件 SHA-256：

| 文件 | SHA-256 |
| --- | --- |
| `Visual.Distance.dll` | `C08865D381F3B732D5E502DF1A3F8761F37E71853D872F3B5B3F72F65ABBBEEB` |
| `Visual.Engine.OpenCv.dll` | `3806F381ED0E79ADD8031075B886B5CB3F359BCF74CF2EC4351732F9E376A703` |
| `OpenCvSharp.dll` | `A26FD4BFFF04FE2A351B3B0B417CC9847EF8E10721A8E484E3B33850F510B509` |
| `OpenCvSharpExtern.dll` | `41E6A418637B7992814A668272E3197306F75C01B151DDB24AC2DE183FB9C3F3` |

首次独立运行发现原生 DLL 仅处于 `runtimes/win-x64/native` 时，原始 DLL 引用项目无法加载 `OpenCvSharpExtern`。消费者随后显式复制原生 DLL 到可执行目录，复测通过。该布局要求已写入 [验收工具说明](../../acceptance/README.md)。

## 4. 未签认原因

本记录没有把单元测试替代为设备验收，也没有把短时运行外推为 10 分钟性能或 2 小时稳定性结论。正式完成 M7.1 至少还需要：

1. USB 相机与真实目标/距离工装；
2. 近、中、远固定数据和 720p 十分钟视频；
3. 恢复 [HardwareModularWorkflow S5 集成待处理事项](../待处理/HardwareModularWorkflow-S5集成.md)，并单独授权修改该仓库；
4. 人工 UI 截图与签认。
